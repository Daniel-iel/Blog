name: github pages

on:
  push:
    tags:
      - v*
      - doc*
    branches: [main]
  pull_request:
    branches: [main, "doc/*"]

jobs:
  gh-release:
    env:
      APP_FOLDER: "./app"

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup Hugo 🔧
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "latest"
          # extended: true

      - name: Build ⚙️
        working-directory: ${{ env.APP_FOLDER }}
        run: hugo -D --minify

      - name: Deploy ✉️
        uses: peaceiris/actions-gh-pages@v3              
        with:
          github_token: ${{ secrets.GH_PAGES_DEPLOY }}
          publish_dir: ${{ env.APP_FOLDER }}/public
